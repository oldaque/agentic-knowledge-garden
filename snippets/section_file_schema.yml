$schema: snippets.v1
file:
  path: snippets/<pattern>/<slug>.md
meta:
  name: string
  objective: string
  how_to_run: string
  from_note: path
rules:
  - size_limit_approx_kb: 30
  - languages_allowed: [python, js, ts, go]
  - format: markdown with codeblocks
